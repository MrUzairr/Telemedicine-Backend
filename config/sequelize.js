// const { Sequelize } = require('sequelize');

// const sequelize = new Sequelize('tasks', 'postgres', '123', {
//     host: 'localhost',
//     dialect: 'postgres',
//     logging: false,
// });
// sequelize.sync({ force:false,alter: true })
//     .then(() => console.log('Database schema synchronized.'))
//     .catch(err => console.error('Error during schema synchronization:', err)); // Use with caution as it drops and recreates the tables

// module.exports = sequelize;
